<?php
?>
<!DOCTYPE html>
<html>
<head>
  <title>4.0 Tasker</title>
  <style>
    body {
      background-color: white; 
      margin: 0;
      overflow: hidden; 
    }
    .letter {
      position: absolute; 
      font-size: 32px;
      font-weight: bold;
      color: black; 
    }
  </style>
</head>
<body>
<?php
$name = "STEVENKANE PABELONA"; 
$letters = str_split($name);
foreach ($letters as $ch) {
    if ($ch !== " ") { 
        echo "<span class='letter'>$ch</span>";
    }
}
?>

<script>
  const letters = document.querySelectorAll(".letter");
  let angle = 0;
  let y = window.innerHeight;

  function animate() {
    y -= 2;
    if (y < -50) {
      y = window.innerHeight; 
    }

    letters.forEach((letter, index) => {
      const offsetAngle = angle + index * 0.3; 
      const xOffset = Math.sin(offsetAngle) * 120;

      letter.style.top = (y - index * 15) + "px"; 
      letter.style.left = (window.innerWidth / 2 + xOffset) + "px";
    });

    angle += 0.05;
    requestAnimationFrame(animate);
  }

  animate();
</script>
</body>
</html>
